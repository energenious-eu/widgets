var tape = require("tape");
// import {getUniqueArrayValues} from "../lib/index";
// import Utils from "../lib/index";

var EmbeddableWidget = require("../lib/index.js");

test('make sure it works', (assert) => {
	assert.ok(EmbeddableWidget)
    assert.end();
});